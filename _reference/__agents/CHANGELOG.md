# CHANGELOG

## Entries
- Dated entries, newest first.

## Entry Template
- YYYY-MM-DD: Short summary of changes or discoveries.
- 2026-02-14: Follow-up list-adapter normalization: removed remaining legacy `foleo-v1-*` style ownership for FOLEOs delete action sizing/icon alignment in `admin-client-wp-list-adapter.css`, now owned by list hook classes (`foleo-list-action-btn--delete`). Legacy `foleo-v1-*` action classes remain JS compatibility markers for variant detection only. Bumped asset version to `0.4.71`.
- 2026-02-14: Global primitive normalization pass: utility button sizing moved to shared component owner (`.foleo-ui-btn--utility` now 31px standard in `admin-client.css`), setup-local utility size override removed from `foleo-v1.css`, FOLEOs list adapter utility selectors switched from legacy `foleo-v1-btn-utility` ownership to global `foleo-ui-btn--utility`, and adapter JS stopped adding the legacy utility class. Content/report headings now explicitly use global typography classes in markup (`foleo-ui-type-subsection` / `foleo-ui-type-metric` / `foleo-ui-type-page`) with page CSS margins targeting those classes. Replaced remaining hard-coded template-meta text colors in `foleo-v1.css` with `--bd-*` tokens. Bumped asset version to `0.4.70`.
- 2026-02-14: Updated `_reference/__agents/ADMIN_CSS_CONTRACT.md` with a required Standard-First Decision Gate: mockup implementation must use existing global standards first (`foleo-ui-*`, `--bd-*`), and any missing style must be explicitly classified as either `Global standard` or `Page-level exception` before adding new style ownership. Added review gate to prevent Layer 3 ad-hoc visual primitive ownership.
- 2026-02-14: Added global FOLEO admin color-token expansion in `assets/css/admin-client.css` for established UI families (feature panel, alerts, delete/danger, success, notes chips, token boxes, scrims/tooltips/spinner tracks, border/text tiers), and rewired `assets/css/foleo-v1.css` standard UI surfaces/states to consume `--bd-*` variables instead of hard-coded literals where applicable. Bumped asset version to `0.4.67`.
- 2026-02-14: Content Upload/Report UX refinement pass: aligned assets-page top header/actions flow to Setup pattern (buttons in row under heading/status), reduced content-shell title scale, converted report source display to comma-separated strings, added auto-included Case Studies and Team Profiles report sections, added token box visuals (green/gray) in scorecard rows, and added icon tooltips for notes/open actions on asset rows. Bumped asset version to `0.4.64`.
- 2026-02-14: Implemented UI-only Content Upload and Content Report flows on `admin.php?page=foleo-assets` with `foleo_step=upload|report|preview` routing, mocked snapshot-driven scorecard/report contracts in `assets/js/foleo-v1.js`, local UI state via `localStorage`, and modal interactions for notes/open/delete/not-needed/proceed confirmation. Added page-root-scoped assets/report styling in `assets/css/foleo-v1.css`. Kept logic non-persistent and bumped asset version to `0.4.63`.
- 2026-02-14: Pass 3B Batch 2 list adapter cleanup: added list-only JS hooks for FOLEOs list title/sort/actions (`foleo-list-title-link`, `foleo-list-row-title`, `foleo-list-sort-indicator`, `foleo-list-action-btn` with variants), migrated Category B button/title/sort styling to those hooks, and removed Category A/B `!important` declarations while keeping Category C table-collision overrides intact. List adapter `!important` count reduced from 44 to 14. Bumped asset version to `0.4.61`.
- 2026-02-14: Fixed Setup header styling regression on `foleo-v1-detail` by re-scoping header-only selectors in `assets/css/foleo-v1.css` from `.foleo-page--detail.foleo-page--setup` to `.foleo-page--detail-shell` so they match header markup outside the detail root. Kept panel/body selectors under detail+setup scope. Bumped asset version to `0.4.60`.
- 2026-02-14: Pass 3A list adapter `!important` reduction (Batch 1): added list-only remap hook classes in `admin-client-wp-adapter.js` (`foleo-list-page-action`, `foleo-list-views`, `foleo-list-utilitybar`, `foleo-list-searchbox`) and re-scoped `admin-client-wp-list-adapter.css` selectors under `body.foleo-client-shell-active.edit-php.post-type-foleo_page .foleo-page--list`. Converted Batch 1 title/views/tablenav/search/utility rules to class-first hooks and reduced list-adapter `!important` count from 119 to 44 while preserving FOLEOs list baseline intent. Bumped asset version to `0.4.58`.
- 2026-02-13: Pass 1+2 page-root alias and CSS gating rollout: added page aliases in FOLEO render roots (`foleo-page--workspace`, `--new`, `--detail`, `--setup`, `--assets`, `--setup-shell`) and optional list alias injection (`foleo-page--list`) in adapter JS. Re-scoped `assets/css/foleo-v1.css` from shell-global selectors to page-root gated modules, replaced brittle `dashboard_page_foleo-v1-detail` selector with alias-based detail-shell selector, and split responsive rules by shared/detail/new gates while keeping a single stylesheet handle. Bumped asset version to `0.4.57`.
- 2026-02-13: Silenced analyzer false-positive in `includes/core/context.php` by replacing direct `BREAKDANCE_IS_EDITING` constant access with `defined(...) && (bool) constant(...)` in `foleo_is_builder_request()`. Runtime behavior unchanged.
- 2026-02-13: Fixed `context.php` function-guard structure by giving `foleo_is_foleo_list_screen()` its own `function_exists` block (separate from `foleo_is_client_shell_context()`) to remove editor/static-analysis ambiguity while preserving runtime behavior.
- 2026-02-13: Option A shell remediation for FOLEOs native list: updated `foleo_is_client_shell_context()` to treat `edit.php?post_type=foleo_page` as client-shell context (screen-specific, including admins), restored strict shell gating on list adapter CSS selectors (`body.foleo-client-shell-active.post-type-foleo_page.edit-php`), restored shell-only JS adapter execution with list remap running only inside shell, and removed non-shell list-adapter enqueue fallback in `theme.php`. Bumped asset version to `0.4.56`.
- 2026-02-13: FOLEOs list restoration pass (screen-scoped): broadened list adapter selectors to `body.post-type-foleo_page.edit-php`, enabled list adapter enqueue/JS remap for FOLEOs list context, restored compact tablenav/search/icon remap behavior, tightened row/button spacing, removed vertical cell borders, and centered delete icon action. Updated delete URL to standard trash flow (`get_delete_post_link($post_id)`). Bumped asset version to `0.4.55`.
- 2026-02-13: Isolated Setup progress styling from FOLEOs list table by removing shell-wide `.tablenav` overrides from `assets/css/admin-client-wp-adapter.css`, removing `tablenav` from Setup progress markup in `includes/admin_client/foleo_v1.php`, and making `.foleo-v1__progress--plain` self-owned in `assets/css/foleo-v1.css` (no WP primitive coupling). Bumped asset version to `0.4.54`.
- 2026-02-13: Reconciled temporary Setup CSS overrides in `foleo-v1.css` by moving them into properly scoped client-shell/detail selectors near the canonical setup/progress blocks and removing the unscoped bottom `/* Jon Added */` section. Bumped asset version to `0.4.49`.
- 2026-02-13: Setup header actions moved into a dedicated row between header info and progress bar (`foleo-v1__setup-actions-row`), right-aligned, with compact vertical spacing to match bottom action rhythm. Updated setup-top grid to two columns (name + status) for better flow. Bumped asset version to `0.4.48`.
- 2026-02-13: Progress bar spacing/color tweak: set connector line to white, added connector offsets to keep 10px clearance around each dot, and increased dot-to-label vertical spacing by 10px for readability.
- 2026-02-13: Setup progress/toggle polish: converted step connectors to per-step segments so the line no longer extends left of `Setup` or right of `Live`, and updated missing-branding checkbox layout so checkbox sits directly beside the `Use default branding` label text on the left.
- 2026-02-13: Tablenav utility scope widened for client-shell screens: added reusable `tablenav` shell styling in Layer 4 adapter CSS and added a Setup progress exception (`.foleo-v1__progress--plain.tablenav`) so progress can intentionally inherit utility-bar background treatment outside the FOLEOs list screen. Bumped asset version to `0.4.45`.
- 2026-02-13: Setup visual consistency pass: enforced page-action hierarchy for Setup header/footer controls (`Save` secondary, `Save & Continue` primary), removed oversized mobile name-field override, and documented the Setup header action rule in `UX-Client-Admin.md`. Bumped asset version to `0.4.41`.
- 2026-02-13: Setup control-style reconciliation pass: replaced remaining WordPress default button styles on Setup with FOLEO UX pattern classes (`foleo-ui-btn--utility`, `foleo-ui-btn--primary`, `foleo-ui-btn--secondary`) so Save/Save & Continue, Add branding, and Select another template match established FOLEOs/Workspace button conventions. Bumped asset version to `0.4.40`.
- 2026-02-13: Setup detail refinement follow-up: removed nested inner panel borders so Branding/Selected Template/Properties render as three clean stacked panels; made progress bar render without panel chrome; tightened header-name field scale and kept status inline in normal flow; switched `Select another template` to utility button style. Bumped asset version to `0.4.39`.
- 2026-02-13: Setup layout refinement pass: moved editable FOLEO name into the top header region (removed duplicate name panel below), moved primary Save/Save & Continue controls above the progress bar, converted status panel into inline header status lines (Status, Draft URL, Live URL, Readiness Score), removed Review Doc block from Setup screen, and restructured Branding + Selected Template panels into a 3-column composition (label/copy/actions). Bumped asset version to `0.4.38`.
- 2026-02-13: Setup detail progress UI pass: converted the step list into a horizontal progress bar and merged `FOLEO Scan` + `Revision` into a single `AI Pass` step (5-step flow: Setup, Assets, AI Pass, Preview & Approval, Live). Kept the right-side `FOLEO Review Status Panel` intact. Bumped asset version to `0.4.37`.
- 2026-02-13: Removed the Add-a-FOLEO intermediate “Name and configure” gate and switched template selection/clone to direct secure POST clone actions with nonce protection, immediately redirecting to Setup (`foleo-v1-detail`). Updated clone entry points on template cards and FOLEOs list actions to submit clone forms instead of `base_id` links. Kept Setup as the single place for naming/properties. Bumped asset version to `0.4.36`.
- 2026-02-13: Setup Step (Build FOLEO) UX/validation implementation: added dual submit intents (`save` vs `save_continue`), blocking red validation for blank FOLEO name on continue, non-blocking orange warning for missing workspace branding, and top status box rendering for setup warnings/errors. Added branding-state logic (workspace branding exists -> inherit/custom controls; missing -> Add branding + Skip copy), selected template summary + repick button, properties panel with Business Type/Intent/SEO Visibility dropdowns, and SEO default behavior to Hidden when unset. Added top and bottom Save & Continue controls, and continue routing to Step 2 (`admin.php?page=foleo-assets&foleo_page_id={id}`) when no blocking errors. Bumped asset version to `0.4.35`.
- 2026-02-13: Add-a-FOLEO page refinement pass: constrained top feature banner to a compact row (`min-height: 60px` + reduced vertical padding), reduced `or` divider visual weight/size to align with nearby heading hierarchy, and added bottom spacing above footer for the add-gate layout (`foleo-v1--new .foleo-v1__main`). Bumped asset version to `0.4.34`.
- 2026-02-13: Reconciled Add-a-FOLEO screen layout to match design flow: full-width feature panel at top, standalone “or” divider, and a single template section with heading + helper copy + unified template grid. Removed the prior split “Your templates / Template library” block structure and replaced with one combined filtered grid while preserving internal base/template routing and model keys. Fixed card compression by using `foleo-v1--new` layout (instead of `foleo-v1--detail`) for the add-gate shell. Bumped asset version to `0.4.33`.
- 2026-02-13: Add-a-FOLEO gate layout pass: introduced reusable feature panel variant `foleo-ui-panel--feature` (Layer 1/2) and applied it to the top scratch section. Updated template cards to reuse recent-card visual pattern (media placeholder + card layout) and render metadata lines `Business type` and `Intent`; action CTAs are now `Preview` (secondary) and `Select` (primary). Bumped asset version to `0.4.32`.
- 2026-02-13: Add-a-FOLEO gate copy pass switched user-facing selection terminology from “Base FOLEO” to “template” while keeping internal model/hooks/routes unchanged. Updated visible strings for page title (`Add a FOLEO`), scratch CTA panel copy (`Schedule a call`), template section intro, card section headers (`Your templates`, `Template library`), and selection button label (`Select template`).
- 2026-02-13: Updated `_reference/UX-Client-Admin.md` to reflect current implementation: Layer 4 adapter split (`admin-client-wp-adapter.css` vs `admin-client-wp-list-adapter.css`), FOLEO list-only gating contract, and explicit Layer 4b nav fallback sublayer constraints (client-shell-only, `#adminmenu` containment, sentinel idempotency, capped retry).
- 2026-02-13: Added a dedicated nav fallback sublayer in `menus.php` (client-shell gated, left-nav-only scope) that reinserts FOLEO logo + Workspace menu-top link with a short retry loop when adapter assets are missing/delayed. Includes minimal fallback logo styling only for `#adminmenu .foleo-admin-logo`. Bumped asset version to `0.4.31`.
- 2026-02-13: Left-nav logo regression fix in Layer 4 adapter JS: added resilient shell adapter execution (initial run + DOMContentLoaded + short retry loop) so `.foleo-admin-logo` insertion and Workspace top-link rewiring persist when admin menu markup is delayed. Bumped asset version to `0.4.30`.
- 2026-02-13: Enforced client-admin 4-layer boundary hardening: fixed Workspace card action duplication (single `Edit` render), removed inline adminmenu/notice `<style>/<script>` hacks from `menus.php`, moved those behaviors into Layer 4 adapter assets, split WP adapter CSS into shell-wide nav adapter (`admin-client-wp-adapter.css`) and FOLEO-list-only adapter (`admin-client-wp-list-adapter.css` loaded only on `edit.php?post_type=foleo_page`), and scoped Layer 1/2 base styles in `admin-client.css` to `.foleo-admin-theme-scope` only. Added adapter JS scope bootstrap that applies theme scope class to native wraps in client-shell. Bumped asset version to `0.4.29`.
- 2026-02-13: Client-shell admin background alignment fix: set `body.foleo-client-shell-active` background to the same tokenized surface as `#wpbody-content` (`--bd-bg`) so both layers render consistently. Bumped asset version to `0.4.28`.
- 2026-02-13: Added client-admin UX baseline spec `UX-Client-Admin.md` and introduced dedicated Layer 4 WP adapter files (`assets/css/admin-client-wp-adapter.css`, `assets/js/admin-client-wp-adapter.js`). Moved left-nav/menu and FOLEOs native list-table remap selectors into adapter CSS, moved FOLEOs list DOM remap JS into adapter JS, and kept `foleo-v1.js` focused on FOLEO component behavior. Bumped asset version to `0.4.27`.
- 2026-02-13: FOLEOs list actions updated: added `Clone` action button (uses existing `foleo-v1-new&base_id={id}` clone flow), widened Actions column for four controls, and increased primary button selector specificity so `Edit` reliably renders blue in action rows. Bumped asset version to `0.4.26`.
- 2026-02-13: FOLEO card layouts now use snapped responsive columns for card grids (`.foleo-v1__card-grid`, `.foleo-v1__picker-grid`) with breakpoints `4/3/2/1` instead of fluid fill behavior; removed previous `<=1200px` forced single-column override for those grids. Bumped asset version to `0.4.25`.
- 2026-02-13: Workspace polish pass: added client-only admin footer override (`admin_footer_text` + empty `update_footer`), added 1:1 placeholder media box to Recent FOLEOs cards, and enforced 60px spacing under Workspace section heads for panel rhythm.
- 2026-02-13: Added reusable page-level action size pattern in `assets/css/admin-client.css` (`foleo-ui-page-action-btn` with `--primary|--secondary` variants) and applied it to Workspace CTAs (`Upload content`, `Create FOLEO`, `Help`) with content-width flex layout for non-full-width actions. Bumped asset version to `0.4.22`.
- 2026-02-13: Pattern rollout proof pass: applied shared `foleo-ui-*` classes to Workspace CTAs/card actions and FOLEO list row actions/page action (`foleo-ui-page-action`, `foleo-ui-btn--primary|secondary|delete|utility`) while retaining existing `foleo-v1-*` classes for compatibility. Asset version now `0.4.2`.
- 2026-02-13: Added shared admin UX pattern layer in `assets/css/admin-client.css`: reusable classes for views tabs, primary page action, utility bar, and button hierarchy (`foleo-ui-btn` + `--secondary|--primary|--delete|--utility`) to support consistent rollout across non-FOLEO admin screens.
- 2026-02-13: Refactored FOLEO list action buttons to reusable class hierarchy: `foleo-v1-btn-action` base with `--secondary`, `--primary`, and `--delete` modifiers; existing action markup now uses these classes while retaining legacy class names for backward compatibility.
- 2026-02-13: FOLEO list visual fix pass: corrected search icon button rendering to a single centered icon (no tiling), changed Edit hover to black border/text, and changed Add Foleo hover to black border/text.
- 2026-02-13: Added reusable utility button style class (`foleo-v1-btn-utility`) and applied it only to FOLEO list Apply/Search submit controls; utility style is light gray fill, gray lowercase text, no border, drop shadow, and darker gray hover. Also removed box-shadow from active top view chip.
- 2026-02-13: FOLEO list correction pass: moved search box into top tablenav actions (icon submit retained), increased selector specificity so compact Apply/Search control styling overrides global admin button defaults, and fixed delete hover to force red border/icon color on the trash action.
- 2026-02-13: FOLEO list layout tweak pass: renamed column label to `Actions`, widened action column for three controls, changed top search submit to icon button, aligned `Add Foleo` to the right of the `All|Published|Trash` row, and added red hover border/text for delete icon button. Bumped asset version to `0.3.7`.
- 2026-02-13: FOLEO list refinement pass: reduced top view tab scale with active white highlight chip, switched top Apply/Search controls to compact blue-outline treatment, enforced secondary styling for Get URL/Preview/View and primary styling for Edit, added delete icon button in Actions column, and tightened SEO On/Off active state to bold black text. Bumped asset version to `0.3.6`.
- 2026-02-13: FOLEO list visual parity pass (reference-driven): removed custom Published/Draft/All button row, hid FOLEO post-state badges under title, switched SEO On/Off back to text-link state treatment, normalized list/table radii (5px panels, 3px buttons), applied white header/rows with thin separators, moved Add FOLEO into top-right search area, and relocated search controls into top tablenav actions. Bumped asset version to `0.3.5` for cache busting.
- 2026-02-13: Bumped `FOLEO_CORE_VERSION` to `0.3.4` to bust cached admin assets after FOLEO list UX/style updates (`foleo-v1.css` / `foleo-v1.js`).
- 2026-02-13: FOLEO list UX restyle/declutter pass in client shell: hid Screen Options/help tabs on `foleo_page` list, emphasized active `All|Published|Trash` view state, added utility button styling for top tablenav controls, switched row utility/actions to secondary button style, strengthened SEO On/Off active visual state, removed zebra striping (white rows with thin separators), and hid duplicate table footer header row.
- 2026-02-13: Decluttered FOLEO list/detail editing path: removed row-actions under FOLEO name for `foleo_page`, mapped edit-post links to OPEN detail route, redirected `post.php` for `foleo_page` back to OPEN detail, and added `FOLEO Name` field to OPEN Setup form with title save on `review_status_save`.
- 2026-02-13: Added FOLEO SEO compatibility bridge: FOLEO SEO toggles now also write legacy `foleo_seo_visibility` (`on`/`off`), and FOLEO SEO reads honor that legacy key as fallback, so legacy and native FOLEO list modes stay consistent.
- 2026-02-13: FOLEO native list (`edit.php?post_type=foleo_page`) now includes wireframe-style columns/actions: SEO Visibility On/Off row toggle (updates `foleo_seo_enabled` + `foleo_public_seo_enabled`), Status, Get URL copy button, and View/Preview/Build primary action based on live/draft availability.
- 2026-02-13: FOLEOs nav now routes to native `edit.php?post_type=foleo_page` list under Workspace, with prominent client-shell Published/Draft/All toggle buttons and query filtering that preserves FOLEO categorization (`publish` OR non-empty `foleo_live_url`).
- 2026-02-13: Client-shell Workspace navigation fix: registered `foleo-workspace` under Dashboard (`index.php`) so the top-level Workspace menu resolves back to Workspace instead of FOLEOs, and updated injected admin logo link target to `admin.php?page=foleo-workspace` (was `edit.php?post_type=page`).
- 2026-02-13: IA stabilization skeleton for client shell: nav now exposes Workspace/FOLEOs/Assets/Setup pages in v1, Workspace was reduced to CTA + recent-card dashboard behavior, FOLEOs became the operations hub with Published/Draft sections and minimal actions, Build screen step labels were aligned to Setup → Assets → FOLEO Scan → Revision → Preview & Approval → Live, Base FOLEO picker switched to a grid card layout, and client top-level Media menu is removed to keep nav focused.
- 2026-02-12: Standard workflow v0 added for FOLEOs: new client-shell chooser route (`admin.php?page=foleo-v1-new`) clones from “Your FOLEOs” or “FOLEO Library” (same CPT + `foleo_is_library_item` meta), persists `foleo_workflow_type`/`foleo_base_foleo_*` linkage, redirects native `post-new.php?post_type=foleo_page` to chooser, and FOLEO Detail now shows base linkage while hiding dev-only mode/config/SEO controls for standard records.
- 2026-02-12: Hardened template-aware Review Doc flows: structured import now surfaces safe detailed missing-field errors, export download handles header/filesize edge cases and tmp cleanup more defensively, and FOLEO detail hides/disables prefilled export when the selected template file is missing.
- 2026-02-12: Template registry contract aligned to locked/packet modes with explicit `docx_template_path`, `export_map`, and `import_map`; FOLEO Review Doc export/import now routes by mode and uses locked mapping validation for structured templates.
- 2026-02-12: Added template-aware Review Doc v0 registry (`foleo_template_id`) with structured/open strategies, prefilled DOCX export by template, strategy-routed DOCX import on upload, strict structured required-field validation, and live draft JSON regeneration + preview updates from latest uploaded review doc.
- 2026-02-12: Draft Preview v1 now regenerates `foleo_draft_json` from the latest uploaded Review Doc (DOCX zip/XML best-effort extraction, deterministic headline/subhead/section heuristics, safe fallback payload on parse failure, and preview meta note `Last generated from Review Doc: vX`).
- 2026-02-12: Draft rewrite activation hardening: `/draft/<token>` now uses a draft-specific `wp_loaded` flush runner that can execute for client-shell users in site admin (not network admin), so pending rewrite flushes no longer depend on a `manage_options` visit.
- 2026-02-12: Added draft preview v0 route `/draft/<token>` backed by `foleo_page` meta (`foleo_draft_token`, `foleo_draft_json`) with safe escaped rendering, 404 handling for invalid tokens, rewrite-version flush signaling, upload-time draft JSON initialization, and FOLEO detail panel Draft URL output.
- 2026-02-12: FOLEO v1 detail route access fix: stopped removing `foleo-v1-detail` submenu registration (route remains registered) and now hides the entry via client-shell admin CSS; unauthorized detail access now returns explicit 403.
- 2026-02-12: FOLEO v1 Detail now supports Review Doc artifact v0 upload/history for `foleo_page` records (docx-only validation, attachment creation via WP media APIs, per-record `foleo_review_doc_versions` versioning, and upload notices + download history in Step Drawer).
- 2026-02-12: FOLEO v1 detail panel now persists review-status meta (`foleo_style`, `foleo_page_set`, `foleo_mode`, `foleo_config_status`, `foleo_seo_enabled`, `foleo_draft_token`, `foleo_readiness_score`) via nonce-protected POST + redirect, with mode-aware SEO toggle and placeholder Draft/Live URL fields.
- 2026-02-12: Legacy submenu nesting CSS now targets href-contains selectors (`edit.php?post_type=page`, `my-sites.php`, `admin.php?page=foleo-settings`) to remain stable across submenu reorder/remove-readd operations.
- 2026-02-12: FOLEOs v1 vertical slice: added `foleo_page` row action “Open”, hidden detail route `admin.php?page=foleo-v1-detail`, scoped v1 enqueues for `foleo_page` list and detail screens only, and added minimal v1 shell assets (`foleo-v1.css`, `foleo-v1.js`).
- 2026-02-12: Added `includes/admin_client/foleo_v1.php` and registered `foleo_page` CPT on `init` (with `post_type_exists` guard) to fix Workspace FOLEOs list showing “Invalid post type.”
- 2026-02-12: Workspace legacy nav ordering: moved legacy item final ordering to late remove/re-add pass so Legacy directly precedes and contains My Foleos/My Sites/Settings; disabled state now removes Legacy header and all legacy submenu entries; added nested legacy submenu styling.
- 2026-02-12: Client shell nav default state: corrected menu default to expanded by setting `mfold` to `o` and `unfold` to `1` in client-shell admin init.
- 2026-02-12: Client shell Workspace nav: added sentinel submenu group headers (FOLEO/Assets/Setup/Legacy), introduced per-site `foleo_legacy_nav_enabled` flag with default true, gated legacy submenu items (My Foleos/My Sites/Settings), and added late submenu cleanup when legacy is disabled.
- 2026-02-11: Compile mode: removed `.compile-type` targeting from `Compile.js` so copy blocks no longer open FOLEO HUD; native Breakdance/custom-field editing is now the default for copy. Documented root cause of missing HUD (`?compile=1` gating and class-based targeting).
- 2026-02-10: Builder iframe videos: z-index fix prevents poster ghosting after hydrate.
- 2026-02-10: Builder iframe videos: edit-mode poster-first flow with click-to-hydrate and mutation-based rescan for CF Stream placeholders.
- 2026-02-09: Consolidated operating docs under `_reference/__agents/`, added `AGENTS.md`, archived duplicate references, and added root stubs pointing to canonical docs.
- 2026-02-09: Compiler UI: Properties auto-closes on responsive menu open to avoid overlay conflict.
- 2026-02-09: Docs: added compiler route + Breakdance injection contract notes and post-update checklist items.
- 2026-02-09: Builder iframe: gated runtime animation bundles (GSAP/ScrollTrigger/Lenis) to prevent console errors.
- 2026-02-09: Builder iframe: narrowed guard to skip runtime JS bundles while allowing safe CSS.
- 2026-02-09: Editor Lottie preview: last-frame placeholder with admin-only preview controls in edit contexts.
- 2026-02-09: Editor Lottie preview enabled in compiler/builder shell for editors (no ScrollTrigger).
